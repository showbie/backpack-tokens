import UIKit

extension UIColor {
    struct Backpack {
{{#each colors as |hue|}}
{{#each hue.scale as |value label|}}
        static let sb_{{hue.name}}_{{label}} = UIColor(rgb: 0x{{hexnum value}})
{{/each}}

{{/each}}
    }
}
